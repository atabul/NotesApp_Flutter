import 'package:flutter/cupertino.dart';

class RectColorBox extends StatelessWidget {
  const RectColorBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
